{
  "name": "shipwrecked-ai",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --cwd packages/bot run dev",
    "start": "bun --cwd packages/bot run start",
    "db:generate": "bun --cwd packages/bot run db:generate",
    "db:migrate": "bun --cwd packages/bot run db:migrate",
    "db:studio": "bun --cwd packages/bot run db:studio"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
